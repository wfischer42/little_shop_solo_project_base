<% if current_user && current_user.merchant? %>
  <%= link_to 'Add New Item', new_item_path %><br>
  <%= link_to 'Stock New Items', unstocked_merchant_items_path(current_user) %><br>
  <% @inv_items.each do |inv_item| %>
    <%= render 'items/item.html.erb', item: inv_item.item, inv_item: inv_item  %>
  <% end %>
<% end %>
